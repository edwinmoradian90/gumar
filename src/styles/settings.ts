import {StyleSheet} from 'react-native';
import {mainText} from '../utils/colors';

const settingsStyles = StyleSheet.create({
  container: {
    flex: 1,
  },
  heading: {
    padding: 16,
  },
});

export default settingsStyles;
